<Window x:Class="KiwiGui.ModelExplorer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KiwiGui"
        mc:Ignorable="d"
        Title="Kiwi 언어모델 탐색기" Height="450" Width="800" Initialized="Window_Initialized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Name="BackwardBtn" Width="20" Height="20" Margin="5" Click="BackwardBtn_Click" ToolTip="Back" IsEnabled="False">
                <Viewbox Width="16" Height="16" Stretch="Uniform">
                    <Canvas Width="10" Height="20">
                        <Path Data="M10,0 L0,10 L10,20"
                              Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                              StrokeThickness="2"
                              StrokeLineJoin="Round"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              Fill="Transparent" />
                    </Canvas>
                </Viewbox>
            </Button>
            <Button Grid.Column="1" Name="ForwardBtn" Width="20" Height="20" Margin="5" Click="ForwardBtn_Click" ToolTip="Forward" IsEnabled="False">
                <Viewbox Width="16" Height="16" Stretch="Uniform">
                    <Canvas Width="10" Height="20">
                        <Path Data="M0,0 L10,10 L0,20"
                              Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                              StrokeThickness="2"
                              StrokeLineJoin="Round"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              Fill="Transparent" />
                    </Canvas>
                </Viewbox>
            </Button>
            <TextBox Grid.Column="2" Name="SearchForm" VerticalAlignment="Center" TextChanged="SearchForm_TextChanged"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="150"/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="3*" MinWidth="150"/>
            </Grid.ColumnDefinitions>
            <Grid>
                <DataGrid Name="MorphemeList" AutoGenerateColumns="False" 
                          EnableRowVirtualization="True" EnableColumnVirtualization="True" 
                          SelectionUnit="FullRow" CanUserAddRows="False" CanUserDeleteRows="False" CanUserSortColumns="False" 
                          SelectionChanged="MorphemeList_SelectionChanged" ItemsSource="{Binding}" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="형태" Binding="{Binding Form}" Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="태그" Binding="{Binding Tag}" Width="Auto" IsReadOnly="True"/>
                        <DataGridTextColumn Header="품사" Binding="{Binding POS}" Width="Auto" IsReadOnly="True"/>
                        <DataGridTextColumn Header="의미번호" Binding="{Binding SenseId}" Width="Auto" IsReadOnly="True"/>
                        <DataGridTextColumn Header="기타" Binding="{Binding Misc}" Width="Auto" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"/>
            <Grid Grid.Column="2" Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <RichTextBox Name="DetailBox" IsReadOnly="True" IsDocumentEnabled="True">
                    <RichTextBox.Document>
                        <FlowDocument>
                            <Paragraph Margin="0">
                                <Run Name="DetailForm" Text="" FontSize="24" FontWeight="Bold"/>
                            </Paragraph>
                            <Section>
                                <Paragraph Margin="0">
                                    <Run Text="ID: "/>
                                    <Run Name="DetailId" Text="" FontWeight="Bold"/>
                                </Paragraph>
                                <Paragraph Margin="0">
                                    <Run Text="태그: "/>
                                    <Run Name="DetailTag" Text="" FontWeight="Bold"/>
                                </Paragraph>
                                <Paragraph Margin="0">
                                    <Run Text="SenseID: "/>
                                    <Run Name="DetailSenseId" Text="" FontWeight="Bold"/>
                                </Paragraph>
                                <Paragraph Margin="0">
                                    <Run Text="LmMorphemeID: "/>
                                    <Hyperlink Name="DetailLmMorphemeIdLink" Click="DetailMorphemeIdLink_Click">
                                        <Run Name="DetailLmMorphemeId" Text="" FontWeight="Bold"/>
                                    </Hyperlink>
                                </Paragraph>
                                <Paragraph Margin="0">
                                    <Run Text="OrigMorphemeID: "/>
                                    <Hyperlink Name="DetailOrigMorphemeIdLink" Click="DetailMorphemeIdLink_Click">
                                        <Run Name="DetailOrigMorphemeId" Text="" FontWeight="Bold"/>
                                    </Hyperlink>
                                </Paragraph>
                                <Paragraph Margin="0">
                                    <Run Text="Dialect: "/>
                                    <Run Name="DetailDialect" Text="" FontWeight="Bold"/>
                                </Paragraph>
                            </Section>
                            <Section>
                                <Paragraph Margin="0,5,0,0">
                                    <Run Text="유사 형태소" FontSize="14"/>
                                </Paragraph>
                                <List Name="DetailSimilarMorphemes" Margin="0">
                                </List>
                            </Section>
                        </FlowDocument>
                    </RichTextBox.Document>
                </RichTextBox>
            </Grid>
        </Grid>
    </Grid>
</Window>
